kind = "web"
previewPath = "/welcome/"
title = "Khalaf Olive Groves — Heritage & Traceability"
version = "1.0.0"
id = "artifacts/public-site"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/welcome/" ]
localPort = 20918

[services.development]
run = "pnpm --filter @workspace/public-site run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/public-site", "run", "build" ]
publicDir = "artifacts/public-site/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "20918"
BASE_PATH = "/welcome/"
